[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "shl-ai-recommender"
version = "0.1.0"
description = "SHL AI Assessment Recommendation System"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["app*"]